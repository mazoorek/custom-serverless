apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: simpleingressroute
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`abc.custom-serverless.com`)
      kind: Rule
      services:
        - name: frontend-service
          port: 8080
